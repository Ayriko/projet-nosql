import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#ffffff', 
        },
        background: {
            default: '#000000', 
        },
        text: {
            primary: '#ffffff',
        },
    },
});

export default theme;